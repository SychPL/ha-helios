name: Validate
on:
  push:
  pull_request:
  schedule:
    - cron: "0 4 * * 1"
jobs:
  hassfest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: home-assistant/actions/hassfest@master
  hacs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: hacs/action@main
        with:
          category: integration
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.14"
      - run: pip install -r requirements_test.txt && python -m pytest -q tests
  panel:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node --check custom_components/helios/panel/helios-panel.js && node --test tests/panel/schema.test.mjs
  import-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.14"
      - run: pip install -q homeassistant==2026.8.3
      - run: PYTHONPATH=. python -c "import custom_components.helios, custom_components.helios.config_flow, custom_components.helios.http, custom_components.helios.media_player"
